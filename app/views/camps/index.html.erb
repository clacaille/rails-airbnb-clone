<h1>La liste des camps disponibles </h1>


<% @camps.each do |camp| %>
<div class="card">
 <div class ="card-title"><%= camp.title %></div>
 <div class= "card-description"> <%= camp.description %>
 <%= camp.price %>$
 </div>
</div>

<% end %>

