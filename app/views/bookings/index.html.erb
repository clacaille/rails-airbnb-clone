<h1>My bookings</h1>

<% @bookings.each do |booking| %>
  <ul>
    <li><%= booking.camp.title %>
        <%= booking.start_date %>
        <%= booking.end_date %>
        <%= booking.accepted %>
        <%= link_to "Edit my booking", edit_booking_path(booking)  %>
    </li>
  </ul>
<% end %>
